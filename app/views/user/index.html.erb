<html>
<h1>PhotoSite User Index Page
</h1>
<table>
  <thead>
  <th>User</th>
  <th>First Name</th>
  <th>Last Name</th>
  </thead>
  <tbody>
  <% @users.each do |user| %>
  <tr>
    <td><%= link_to user.id, link_photos_path(user.id) %></td>
    <td><%=link_to user.first_name, link_photos_path(user.id)%></td>
    <td><%=link_to user.last_name, link_photos_path(user.id)%></td>
  </tr>
  <%end%>
  </tbody>
</table>
</html>
